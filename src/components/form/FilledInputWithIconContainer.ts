import styled from 'styled-components';

export const FilledInputWithIconContainer = styled.div`
    display: flex;
    flex-direction: row;
    border-radius: 20px;
    background-color: lightgray;
    border: 1px solid lightgray;
    transition: border 0.6s;
`
