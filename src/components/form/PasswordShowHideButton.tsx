import styled from 'styled-components';

export const PasswordShowHideButton = styled.button`
    background-color: transparent;
    border: none;
    cursor: pointer;
    &:focus {
        outline: none;
    }
`